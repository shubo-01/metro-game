# 寻仙 · 前端全场景搭建指南（Cocos Creator 3.8 可执行版）

> 本文档基于当前仓库最新代码全量梳理生成（已包含 **人物属性 V2** 前端接入：加点/洗点/异常状态/护盾/神魔之道）。
> 目标：在 Cocos Creator 3.8.x 编辑器中，按步骤完成整个项目所有前端场景与 UI 面板的搭建。
> 每一步均给出：**节点树（可照抄创建）→ 组件挂载 → Inspector 属性绑定表 → 验证方法**。
>
> 适用工程：`e:\ziji-xiaochengxu\frontend`（用 Cocos Creator 打开该目录）

---

## 目录

1. [第〇章 前置准备与架构总览（含 TestScene）](#第〇章-前置准备与架构总览)
2. [第一章 Login 登录场景](#第一章-login-登录场景)
3. [第二章 CharacterCreate 角色创建场景](#第二章-charactercreate-角色创建场景)
4. [第三章 Hall 大厅场景（主场景）](#第三章-hall-大厅场景主场景)
5. [第四章 角色面板 CharacterPanel（含 V2 扩展区）](#第四章-角色面板-characterpanel)
6. [第五章 境界突破面板 RealmBreakthroughUI（含 V2 道进度区）](#第五章-境界突破面板-realmbreakthroughui)
7. [第六章 花果山副本三件套（入口/战斗/结算）](#第六章-花果山副本三件套入口战斗结算)
8. [第七章 死亡系统四面板（六道/兽身/夺舍/公敌）](#第七章-死亡系统四面板六道兽身夺舍公敌)
9. [第八章 面板层级与穿透控制](#第八章-面板层级与穿透控制)
10. [第九章 人物属性 V2 三大新面板（加点/洗点/异常状态）](#第九章-人物属性-v2-三大新面板加点洗点异常状态)
11. [第十章 V2 接口一览与 CharacterV2Event 事件总线](#第十章-v2-接口一览与-characterv2event-事件总线)
12. [第十一章 编译、预览与联调验证](#第十一章-编译预览与联调验证)
13. [第十二章 Inspector 绑定速查总表](#第十二章-inspector-绑定速查总表)
14. [附录 待开发部分（野怪/装备 UI）](#附录-待开发部分野怪装备-ui)

---

## 第〇章 前置准备与架构总览

### 0.1 环境要求

| 项目 | 要求 |
|------|------|
| Cocos Creator | 3.8.x（项目使用 3.8.8） |
| 打开方式 | Cocos Dashboard → 打开项目 → 选择 `e:\ziji-xiaochengxu\frontend` |
| 后端服务 | 8001 auth / 8005 character / 8006 death / 8007 dungeon / 8008 monster / 8009 equipment |
| 数据库 | MySQL `game_main` + Redis 127.0.0.1:6379 |

启动后端（每个服务一个终端，工作目录 `e:\ziji-xiaochengxu\backend`）：

```powershell
go run ./cmd/auth-service
go run ./cmd/character-service
go run ./cmd/death-service
go run ./cmd/dungeon-service
go run ./cmd/monster-service     # 首次需 POST /monster/admin/init_world 初始化世界（幂等）
go run ./cmd/equipment-service
```

### 0.2 场景流转总览

```
Login.scene ──登录成功(无角色)──▶ CharacterCreate.scene ──创建成功──▶ Hall.scene
     │                                                                  │
     └──────────────登录成功(已有角色)──────────────────────────────────┘
Hall.scene 内：花果山入口面板 → HGS_ENTER 事件 → 战斗层(BattleLayer) → 结算面板 → 回大厅
```

- 场景切换统一走 `SceneManager.loadScene(SceneName.Xxx)`（`scripts/manager/SceneManager.ts`）。
- 场景名常量：`Login` / `CharacterCreate` / `Hall`，**创建场景文件时名称必须与之完全一致**。

### 0.3 脚本目录结构（均已存在，搭建时只需挂载与绑定）

```
assets/scripts/
├── GameManager.ts              全局单例，常驻节点（persistRootNode）
├── common/Constants.ts         服务器地址/主题色/境界名等常量
├── manager/                    SceneManager / EventManager / TokenManager / PlayerManager
├── net/                        HttpClient / WsClient / CharacterApi(V2新增) /
│                               DungeonApi / MonsterApi / EquipmentApi
├── scene/
│   ├── LoginPanel.ts           登录面板控制
│   ├── CharacterCreatePanel.ts 角色创建控制
│   ├── Hall/HallScene.ts       大厅场景控制
│   ├── Hall/HallUI.ts          大厅 UI 层
│   ├── Hall/PlayerEntity.ts    玩家实体（代码动态创建，无需手动挂）
│   ├── Hall/NPCEntity.ts       NPC 实体（代码动态创建，无需手动挂）
│   └── Huaguoshan/HuaguoshanBattle.ts 花果山战斗层
├── typings/cc.d.ts             编辑器外类型声明（V2 补充了 Node.getComponentInChildren）
└── ui/                         CharacterPanel / RealmBreakthroughUI / DeathChoiceUI /
                                BeastTransformUI / PossessUI / PublicEnemyIndicator /
                                HuaguoshanEntryUI / HuaguoshanSettleUI /
                                AttributeAllocationUI(V2新增) / WashPointUI(V2新增) /
                                AbnormalStatusUI(V2新增)
```

### 0.4 通用搭建规范（全文档适用）

1. **节点命名必须与文档一致**：脚本中部分逻辑按名称查找子节点（如 `LoginPanel` 查找 `CloseBtn`）。
2. **Label 统一设置**：字体留空（系统字体）、`CacheMode = NONE`、颜色可先默认（多数脚本运行时会覆盖颜色/文字）。
3. **按钮 = Node + Sprite(底) + Label(字)**：所有按钮事件均由脚本 `onLoad` 中 `node.on(TOUCH_END)` 代码绑定，**不需要在 Inspector 配置 Button ClickEvents**。给按钮节点加 `Button` 组件仅为了按压变色反馈（可选）。跨面板动作（如"加点"按钮打开加点面板）统一走 `EventManager` 事件总线，禁止面板间直接互相调用。
4. **面板初始隐藏**：弹窗类面板（BindPhonePanel、CreatingPanel、第四~七章各 ui/ 面板）创建后在 Inspector 取消勾选节点 active，或由脚本控制。**例外：第九章的三个 V2 面板必须保持勾选 active**，原因见第九章的 ⚠️注意。
5. **Toast 标签**：每个面板都有 `toastLabel`，初始 active=false，脚本会自动显示/隐藏。
6. **设计分辨率**：项目 4 个场景的 Canvas 均为 **1280×720**（Fit Width + Fit Height 同时勾选）。新建场景时保持与之一致。

### 0.5 TestScene 冒烟测试场景（已存在，无需搭建）

`assets/scenes/TestScene.scene` 是工程创建初期的最小验证场景，**不参与游戏流程，不挂任何自定义脚本**，仅用于确认引擎渲染正常。节点树如下（如需重建照抄即可）：

```
TestScene (Scene)
└── Canvas                          (1280×720，Fit Width + Fit Height)
    ├── CameraNode                  (Node+Camera，正交，背景色深灰 #333333)
    └── TestLabel                   (Label "Hello XunXian!"，金色 #FFD700，60号加粗，400×80)
```

验证：直接预览该场景，屏幕中央显示金色 "Hello XunXian!" 即引擎环境正常。日常开发无需改动它。

---

## 第一章 Login 登录场景

> 状态：**场景文件已存在**（`assets/scenes/Login.scene`）。本章内容用于核对/重建。
> 控制脚本：`scripts/scene/LoginPanel.ts`（挂在 Canvas 上）。

### 1.1 节点树（与现有 .scene 文件一致）

```
Login (Scene)
└── Canvas                          ← 挂 LoginPanel 组件
    ├── Background                  (Sprite，深色底 #1a1a2e，铺满)
    ├── TitleLabel                  (Label，游戏名"寻仙"，脚本设为金色80号)
    ├── SloganLabel                 (Label，标语，脚本自动填充)
    ├── VersionLabel                (Label，版本号，脚本自动填充)
    ├── ServerStatusLabel           (Label，服务器状态"检测中...")
    ├── WxLoginBtn                  (Node+Sprite 按钮底)
    │   └── Label                   (Label，脚本运行时设为"微信一键登录")
    ├── PhoneLoginBtn               (Node+Sprite 按钮底)
    │   └── Label                   (Label，脚本运行时设为"手机号登录")
    ├── BindPhonePanel              (Node，弹窗容器，初始 active=false)
    │   ├── PhoneInput              (EditBox，11位手机号)
    │   ├── CodeInput               (EditBox，6位验证码)
    │   ├── SendCodeBtn             (Node+Sprite)
    │   │   └── Label               (Label ←绑定到 sendCodeBtnLabel)
    │   └── CloseBtn                (Node，脚本按名称查找，勿改名)
    ├── ConfirmBindPanel            (Node，跨平台确认弹窗，初始 active=false)
    └── ToastLabel                  (Label，初始 active=false)
```

### 1.2 搭建步骤

1. 资源管理器 `assets/scenes` 右键 → 新建 → Scene，命名 `Login`（若已存在跳过）。
2. 选中 `Canvas` → Inspector → 添加组件 → 自定义脚本 → `LoginPanel`。
3. 按 1.1 节点树逐个创建子节点：
   - 右键 Canvas → 创建 → 2D 对象 → Sprite（Background/按钮底）。
   - 右键 Canvas → 创建 → 2D 对象 → Label（各文本）。
   - 右键 Canvas → 创建 → UI 组件 → EditBox（PhoneInput/CodeInput）。
4. 推荐布局（1280×720 画布，坐标为节点 Position）：
   - TitleLabel (0, 180)，SloganLabel (0, 110)
   - WxLoginBtn (0, -40) 尺寸 320×72，PhoneLoginBtn (0, -140) 尺寸 320×72
   - ServerStatusLabel (0, -240)，VersionLabel (0, -290)
   - BindPhonePanel 居中 (0,0) 尺寸 520×360；内部 PhoneInput (0,80)、CodeInput (0,10)、SendCodeBtn (170,10)、CloseBtn (235,155)
   - ToastLabel (0, 250)

### 1.3 Inspector 绑定表（选中 Canvas → LoginPanel 组件）

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| titleLabel | Label | Canvas/TitleLabel |
| sloganLabel | Label | Canvas/SloganLabel |
| versionLabel | Label | Canvas/VersionLabel |
| serverStatusLabel | Label | Canvas/ServerStatusLabel |
| wxLoginBtn | Node | Canvas/WxLoginBtn |
| phoneLoginBtn | Node | Canvas/PhoneLoginBtn |
| bindPhonePanel | Node | Canvas/BindPhonePanel |
| phoneInput | EditBox | BindPhonePanel/PhoneInput |
| codeInput | EditBox | BindPhonePanel/CodeInput |
| sendCodeBtnLabel | Label | BindPhonePanel/SendCodeBtn/Label |
| bindConfirmLabel | Label | ConfirmBindPanel 内的确认文本 Label |
| confirmBindPanel | Node | Canvas/ConfirmBindPanel |
| toastLabel | Label | Canvas/ToastLabel |

### 1.4 验证

- 编辑器预览（浏览器）：显示"寻仙"金色标题呼吸动画；点"微信一键登录"→ 提示 `[开发模式] 模拟微信登录...` → 1.5 秒后跳转 CharacterCreate。
- 若 auth-service(8001) 已启动，ServerStatusLabel 显示在线状态。

---

## 第二章 CharacterCreate 角色创建场景

> 状态：**场景文件已存在**（`assets/scenes/CharacterCreate.scene`）。
> 控制脚本：`scripts/scene/CharacterCreatePanel.ts`（挂在 Canvas 上）。
> 流程：性别选择 → 属性展示+命名 → 创建动画（三阶段由脚本切换面板显隐）。

### 2.1 节点树（与现有 .scene 文件一致）

```
CharacterCreate (Scene)
└── Canvas                          ← 挂 CharacterCreatePanel 组件
    ├── Background                  (Sprite，深色底)
    ├── StepTitleLabel              (Label，步骤标题)
    ├── StepDescLabel               (Label，步骤描述)
    ├── GenderPanel                 (Node，第一步容器)
    │   ├── MaleBtn                 (Node+Sprite)
    │   │   └── Label               (Label "男")
    │   └── FemaleBtn               (Node+Sprite)
    │       └── Label               (Label "女")
    ├── MaleSelectedLabel           (Label，选中提示)
    ├── FemaleSelectedLabel         (Label，选中提示)
    ├── AttrsPanel                  (Node，第二步容器，初始 active=false)
    │   ├── AttrsJingLabel          (Label "精: 1")
    │   ├── AttrsQiLabel            (Label "气: 0")
    │   ├── AttrsShenLabel          (Label "神: 1")
    │   ├── AttrsLuckLabel          (Label "气运: 0")
    │   ├── AttrsSavvyLabel         (Label "悟性: 0")
    │   └── AttrsHintLabel          (Label，属性说明)
    ├── NameInput                   (EditBox，2-8 汉字)
    ├── RandomNameBtn               (Node+Sprite，随机名)
    ├── NextBtn                     (Node+Sprite，下一步/创建)
    ├── CreatingPanel               (Node，第三步容器，初始 active=false)
    │   └── CreatingLabel           (Label "塑造仙体中...")
    └── ToastLabel                  (Label，初始 active=false)
```

### 2.2 搭建步骤

1. 新建场景 `CharacterCreate`（若已存在跳过），Canvas 挂 `CharacterCreatePanel`。
2. 布局建议：
   - StepTitleLabel (0, 240)、StepDescLabel (0, 190)
   - GenderPanel 居中；MaleBtn (-120, 0) 160×200，FemaleBtn (120, 0) 160×200
   - AttrsPanel 居中；五个属性 Label 纵向排列 (0, 120→-40，间隔 40)，AttrsHintLabel (0, -90)
   - NameInput (0, -150) 尺寸 360×56；RandomNameBtn (230, -150) 100×56
   - NextBtn (0, -240) 240×64；CreatingPanel 全屏遮罩，CreatingLabel 居中
3. AttrsPanel、CreatingPanel、NameInput、RandomNameBtn、NextBtn 初始 active 由脚本控制，可全部保持勾选（脚本 onLoad 会切到第一步）。

### 2.3 Inspector 绑定表（选中 Canvas → CharacterCreatePanel 组件）

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| stepTitleLabel | Label | Canvas/StepTitleLabel |
| stepDescLabel | Label | Canvas/StepDescLabel |
| maleBtn | Node | GenderPanel/MaleBtn |
| femaleBtn | Node | GenderPanel/FemaleBtn |
| genderPanel | Node | Canvas/GenderPanel |
| maleSelectedLabel | Label | Canvas/MaleSelectedLabel |
| femaleSelectedLabel | Label | Canvas/FemaleSelectedLabel |
| attrsPanel | Node | Canvas/AttrsPanel |
| attrsJingLabel | Label | AttrsPanel/AttrsJingLabel |
| attrsQiLabel | Label | AttrsPanel/AttrsQiLabel |
| attrsShenLabel | Label | AttrsPanel/AttrsShenLabel |
| attrsLuckLabel | Label | AttrsPanel/AttrsLuckLabel |
| attrsSavvyLabel | Label | AttrsPanel/AttrsSavvyLabel |
| attrsHintLabel | Label | AttrsPanel/AttrsHintLabel |
| nameInput | EditBox | Canvas/NameInput |
| randomNameBtn | Node | Canvas/RandomNameBtn |
| nextBtn | Node | Canvas/NextBtn |
| creatingPanel | Node | Canvas/CreatingPanel |
| creatingLabel | Label | CreatingPanel/CreatingLabel |
| toastLabel | Label | Canvas/ToastLabel |

### 2.4 验证

- 预览：点男/女 → 自动进入属性页（精1 神1 气0）→ 输入 2-8 汉字名 → 点"下一步"调用 `POST /player/create` → 成功后播放创建动画并跳转 Hall。

---

## 第三章 Hall 大厅场景（主场景）

> 状态：**场景文件已存在**（`assets/scenes/Hall.scene`）。
> 控制脚本：`HallScene.ts`（世界层控制）+ `HallUI.ts`（UI 层）。
> 玩家/NPC 实体节点由代码动态创建，**无需手动搭建**。

### 3.1 节点树（与现有 .scene 文件一致）

```
Hall (Scene)
└── Canvas                          ← 挂 HallScene 组件
    ├── WorldNode                   (Node，世界容器，尺寸 3000×2000)
    │   ├── GroundLayer             (Node+Graphics，地面层，接收点击移动)
    │   ├── EntityContainer         (Node，实体容器：玩家/NPC/远程玩家动态生成)
    │   └── CameraNode              (Node，相机跟随锚点)
    └── HallUINode                  (Node，UI 层)  ← 挂 HallUI 组件
        ├── PlayerNameLabel         (Label，左上角玩家名)
        ├── PlayerLevelLabel        (Label，境界文本)
        ├── AttrsLabel              (Label，属性快览"精x 气x 神x")
        ├── ToastLabel              (Label，初始 active=false)
        ├── ReconnectLabel          (Label，重连提示，初始 active=false)
        ├── ChatLabel               (Label，聊天记录，左下角)
        ├── FuncEntryContainer      (Node，右侧功能入口容器)
        └── QuickActionContainer    (Node，底部快捷操作容器)
```

### 3.2 搭建步骤

1. 新建场景 `Hall`（若已存在跳过）。
2. Canvas 挂 `HallScene`；创建 `HallUINode` 挂 `HallUI`。
3. `GroundLayer` 必须添加 `Graphics` 组件并将 `ContentSize` 设为 3000×2000（用于接收 TOUCH_END 点击移动）。
4. UI 布局建议（基于 1280×720 屏幕坐标，使用 Widget 对齐）：
   - PlayerNameLabel：左上 (-460, 300)，Widget 左上对齐
   - PlayerLevelLabel：(-460, 270)；AttrsLabel：(-460, 240)
   - ChatLabel：左下 (-460, -200)，行高 20，最多 10 行
   - FuncEntryContainer：右侧 (420, 0)，垂直 Layout
   - QuickActionContainer：底部 (0, -290)，水平 Layout
   - ToastLabel (0, 200)；ReconnectLabel (0, 250)
5. **GameManager 常驻节点**：在 Login 场景 Canvas 下创建空节点 `GameManagerNode` 挂 `GameManager` 脚本（onLoad 中会 `addPersistRootNode` 跨场景常驻，单例保护防止重复）。

### 3.3 Inspector 绑定表

**Canvas → HallScene 组件：**

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| worldNode | Node | Canvas/WorldNode |
| cameraNode | Node | WorldNode/CameraNode |
| entityContainer | Node | WorldNode/EntityContainer |
| groundLayer | Node | WorldNode/GroundLayer |
| hallUI | HallUI | Canvas/HallUINode（拖节点后自动识别组件） |

**HallUINode → HallUI 组件：**

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| playerNameLabel | Label | HallUINode/PlayerNameLabel |
| playerLevelLabel | Label | HallUINode/PlayerLevelLabel |
| attrsLabel | Label | HallUINode/AttrsLabel |
| toastLabel | Label | HallUINode/ToastLabel |
| reconnectLabel | Label | HallUINode/ReconnectLabel |
| chatLabel | Label | HallUINode/ChatLabel |
| funcEntryContainer | Node | HallUINode/FuncEntryContainer |
| quickActionContainer | Node | HallUINode/QuickActionContainer |

### 3.4 验证

- 预览进入 Hall：自动 `POST /scene/enter`，出现金色描边的玩家剪影 + 3 个 NPC（张真人/药铺掌柜/铸器师）。
- 点击地面任意位置 → 角色移动 + 相机平滑跟随；靠近 NPC 点击 → 弹出对话 Toast。
- WebSocket(8004) 未启动时会显示"网络断开，正在重连..."，属正常现象。

---

## 第四章 角色面板 CharacterPanel

> 状态：**脚本已存在（`scripts/ui/CharacterPanel.ts`），场景中无挂载点，需要新建面板节点。**
> 挂载位置：Hall.scene 的 Canvas 下（与 HallUINode 同级）。
> 打开方式：脚本对外提供 `show()`；由 HallUI 功能按钮"角色"接线调用（见第八章 8.2）。
> **V2 新增**：护盾/五行亲和/反应/异常抵抗/均衡状态 5 个展示项 + "加点"/"洗点"两个入口按钮。

### 4.1 节点树（在 Hall.scene 的 Canvas 下新建）

```
Canvas
└── CharacterPanelNode              (Node，初始 active=false) ← 挂 CharacterPanel 组件
    ├── Mask                        (Sprite，全屏半透明黑 #000000 180，加 BlockInputEvents 组件)
    ├── PanelBg                     (Sprite，720×560，深色底 #1e1e32)
    ├── TitleLabel                  (Label "角色属性"，24号)
    ├── BaseAttrSection             (Node，基础五维区)
    │   ├── JingLabel               (Label "精: --")
    │   ├── QiLabel                 (Label "气: --")
    │   ├── ShenLabel               (Label "神: --")
    │   ├── QiYunLabel              (Label "气运: --")
    │   └── WuXingLabel             (Label "悟性: --")
    ├── DerivedSection              (Node，衍生属性区)
    │   ├── HpLabel                 (Label "气血: --")
    │   ├── MpLabel                 (Label "灵力: --")
    │   ├── SoulLabel               (Label "魂力: --")
    │   ├── PhysiqueLabel           (Label "体魄: --")
    │   ├── AgilityLabel            (Label "身法: --")
    │   ├── SkillPowerLabel         (Label "功法威力: --")
    │   └── SenseRangeLabel         (Label "神识范围: --")
    ├── V2Section                   (Node，V2 衍生值区，V2 新增)
    │   ├── ShieldLabel             (Label "护盾: --/--")
    │   ├── AffinityLabel           (Label "五行亲和: --")
    │   ├── ReactionLabel           (Label "反应: --")
    │   ├── AbnormalResistLabel     (Label "异常抵抗: --")
    │   └── EquilibriumLabel        (Label "均衡: --"，脚本自动填"均衡加成 ×2"或"偏科Build ×1")
    ├── V2Buttons                   (Node，V2 入口按钮区，V2 新增)
    │   ├── AllocateBtn             (Node+Sprite，"加点"，尺寸 120×48)
    │   │   └── Label               (Label "加点")
    │   └── WashBtn                 (Node+Sprite，"洗点"，尺寸 120×48)
    │       └── Label               (Label "洗点")
    ├── RealmSection                (Node，境界区)
    │   ├── RealmLabel              (Label "境界: --")
    │   ├── ExpProgressBar          (Node，进度条容器)
    │   │   ├── BarBg               (Sprite，300×16，灰底)
    │   │   └── BarFill             (Sprite，锚点X=0，脚本按比例缩放宽度)
    │   └── ExpLabel                (Label "0 / 0")
    ├── ElementSection              (Node，五行区)
    │   ├── ElementCountLabel       (Label "兼修: --种")
    │   ├── ExpMultiplierLabel      (Label "经验倍率: ×--")
    │   ├── DamageMultiplierLabel   (Label "伤害倍率: ×--")
    │   └── MultiplierInfoLabel     (Label，五行修炼列表，多行)
    ├── HiddenSection               (Node，隐藏属性区)
    │   ├── XinmoLabel              (Label "心魔: --")
    │   ├── KarmaLabel              (Label "因果: --")
    │   └── DaoXingLabel            (Label "道行: --")
    ├── CloseBtn                    (Node+Sprite "×"，脚本按名称查找，勿改名)
    └── ToastLabel                  (Label，初始 active=false)
```

### 4.2 搭建步骤

1. Hall.scene 中右键 Canvas → 创建空节点，命名 `CharacterPanelNode`，Position (0,0)。
2. 添加组件 → 自定义脚本 → `CharacterPanel`。
3. 按 4.1 创建子节点。推荐布局（PanelBg 内相对坐标）：
   - TitleLabel (0, 250)
   - BaseAttrSection (-220, 140)：五个 Label 纵向排列，间距 40
   - DerivedSection (0, 140)：七个 Label 纵向排列，间距 36
   - V2Section (0, -120)：五个 Label 纵向排列，间距 32
   - V2Buttons (-220, -220)：AllocateBtn (-70, 0)，WashBtn (70, 0)
   - RealmSection (220, 160)：RealmLabel (0,0)、ExpProgressBar (0,-40)、ExpLabel (0,-70)
   - ElementSection (220, -40)：四个 Label 纵向间距 32
   - HiddenSection (-220, -120)：三个 Label 纵向间距 36
   - CloseBtn (330, 250)，尺寸 48×48
   - ToastLabel (0, -240)
4. Mask 节点添加 `BlockInputEvents` 组件（防止点穿到大厅地面）。
5. 设置 CharacterPanelNode 的 active = false（Inspector 取消勾选）。

> ⚠️注意：AllocateBtn / WashBtn 点击后**不直接打开面板**，而是分别 `EventManager.emit(CharacterV2Event.OPEN_ALLOCATE)` / `emit(OPEN_WASH)`，由第九章的加点面板、洗点确认框各自监听后自行打开。这是项目规范（按钮事件必须走事件总线），所以**必须先完成第九章的两个面板搭建，这两个按钮才有效果**。

### 4.3 Inspector 绑定表（选中 CharacterPanelNode → CharacterPanel 组件，共 30 项）

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| jingLabel | Label | BaseAttrSection/JingLabel |
| qiLabel | Label | BaseAttrSection/QiLabel |
| shenLabel | Label | BaseAttrSection/ShenLabel |
| qiYunLabel | Label | BaseAttrSection/QiYunLabel |
| wuXingLabel | Label | BaseAttrSection/WuXingLabel |
| hpLabel | Label | DerivedSection/HpLabel |
| mpLabel | Label | DerivedSection/MpLabel |
| soulLabel | Label | DerivedSection/SoulLabel |
| physiqueLabel | Label | DerivedSection/PhysiqueLabel |
| agilityLabel | Label | DerivedSection/AgilityLabel |
| skillPowerLabel | Label | DerivedSection/SkillPowerLabel |
| senseRangeLabel | Label | DerivedSection/SenseRangeLabel |
| realmLabel | Label | RealmSection/RealmLabel |
| expProgressBar | Node | RealmSection/ExpProgressBar |
| expLabel | Label | RealmSection/ExpLabel |
| elementCountLabel | Label | ElementSection/ElementCountLabel |
| expMultiplierLabel | Label | ElementSection/ExpMultiplierLabel |
| damageMultiplierLabel | Label | ElementSection/DamageMultiplierLabel |
| multiplierInfoLabel | Label | ElementSection/MultiplierInfoLabel |
| xinmoLabel | Label | HiddenSection/XinmoLabel |
| karmaLabel | Label | HiddenSection/KarmaLabel |
| daoXingLabel | Label | HiddenSection/DaoXingLabel |
| **shieldLabel** (V2) | Label | V2Section/ShieldLabel |
| **affinityLabel** (V2) | Label | V2Section/AffinityLabel |
| **reactionLabel** (V2) | Label | V2Section/ReactionLabel |
| **abnormalResistLabel** (V2) | Label | V2Section/AbnormalResistLabel |
| **equilibriumLabel** (V2) | Label | V2Section/EquilibriumLabel |
| **allocateBtn** (V2) | Node | V2Buttons/AllocateBtn |
| **washBtn** (V2) | Node | V2Buttons/WashBtn |
| toastLabel | Label | CharacterPanelNode/ToastLabel |

### 4.4 验证

- 大厅中点击"角色"功能按钮（第八章接线后）→ 面板弹出，自动请求 `GET /character/attributes`、`GET /character/qi/elements`、`GET /character/info`、`GET /character/shield`（V2）四个接口填充全部数值。
- V2 区：护盾显示 `护盾: 当前/上限`；均衡标签在精气神 max÷min≤3 时显示绿色"均衡加成 ×2"，否则灰色"偏科Build ×1"。
- 点"加点"/"洗点"按钮 → 分别弹出第九章的加点面板 / 洗点确认框（需先搭好第九章）。
- 加点/洗点成功后本面板监听 `ATTR_UPDATED` 事件自动整体刷新，无需手动关开。
- 点击 CloseBtn 面板关闭。
- character-service(8005) 未启动时显示 Toast 错误提示，面板数值保持 "--"。

---

## 第五章 境界突破面板 RealmBreakthroughUI

> 状态：**脚本已存在（`scripts/ui/RealmBreakthroughUI.ts`），需新建面板节点。**
> 挂载位置：Hall.scene 的 Canvas 下。由 HallUI"功法"按钮接线打开（见第八章 8.2）。
> **V2 新增**：八大境界名（人/真人/地仙/天仙/金仙/太乙金仙/大罗金仙/神魔）；
> `major_stage=8`（神魔境界）时显示"道进度区"，可进行子阶突破（太极→太素→太始→太初→太易，以道证道）。

### 5.1 节点树

```
Canvas
└── RealmPanelNode                  (Node，初始 active=false) ← 挂 RealmBreakthroughUI 组件
    ├── Mask                        (Sprite，全屏半透明黑，加 BlockInputEvents)
    ├── PanelBg                     (Sprite，560×480，深色底)
    ├── TitleLabel                  (Label "境界修炼")
    ├── CurrentRealmLabel           (Label "当前境界: --")
    ├── NextRealmLabel              (Label "下一境界: --")
    ├── RequirementLabel            (Label，突破条件说明)
    ├── UpgradeBtn                  (Node+Sprite，"修炼升级"，Position (-120, -120)，尺寸 180×64)
    │   └── Label
    ├── BreakthroughBtn             (Node+Sprite，"境界突破"，Position (120, -120)，尺寸 180×64)
    │   └── Label
    ├── DaoSection                  (Node，V2 道进度区容器，非神魔境界时脚本自动隐藏)
    │   ├── DaoProgressLabel        (Label "太极之道: --/100")
    │   └── DaoBreakthroughBtn      (Node+Sprite，"子阶突破"，尺寸 180×56)
    │       └── Label               (Label "子阶突破")
    ├── ResultLabel                 (Label，突破结果，初始 active=false)
    ├── AnimNode                    (Node，突破动画容器，初始 active=false)
    ├── CloseBtn                    (Node+Sprite "×"，脚本按名称查找，勿改名)
    └── ToastLabel                  (Label，初始 active=false)
```

### 5.2 搭建步骤

1. Canvas 下创建空节点 `RealmPanelNode`，挂 `RealmBreakthroughUI` 组件。
2. 布局：TitleLabel (0,200)、CurrentRealmLabel (0,130)、NextRealmLabel (0,80)、RequirementLabel (0,20)、DaoSection (0,-50)（内部 DaoProgressLabel (0,20)、DaoBreakthroughBtn (0,-30)）、ResultLabel (0,-60)、AnimNode (0,0)、CloseBtn (250,200)、ToastLabel (0,-200)。
3. 面板与 AnimNode、ResultLabel、ToastLabel 初始 active=false。**DaoSection 可保持勾选**——脚本 `_refreshInfo()` 会按当前大境界自动控制显隐（`major_stage < 8` 时隐藏）。

> ⚠️注意：道值（太极/太素/太始/太初/太易之道）**没有单独的查询接口**。面板通过监听 `CharacterV2Event.DAO_UPDATED` 事件缓存道值（战斗/任务处调用 `daoGain` 成功后广播），**在收到第一次 DAO_UPDATED 之前，道进度固定显示 `--/100` 占位，属正常现象**，不是搭建错误。

### 5.3 Inspector 绑定表（共 11 项）

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| currentRealmLabel | Label | RealmPanelNode/CurrentRealmLabel |
| nextRealmLabel | Label | RealmPanelNode/NextRealmLabel |
| requirementLabel | Label | RealmPanelNode/RequirementLabel |
| resultLabel | Label | RealmPanelNode/ResultLabel |
| upgradeBtn | Node | RealmPanelNode/UpgradeBtn |
| breakthroughBtn | Node | RealmPanelNode/BreakthroughBtn |
| animNode | Node | RealmPanelNode/AnimNode |
| **daoSectionNode** (V2) | Node | RealmPanelNode/DaoSection |
| **daoProgressLabel** (V2) | Label | DaoSection/DaoProgressLabel |
| **daoBreakthroughBtn** (V2) | Node | DaoSection/DaoBreakthroughBtn |
| toastLabel | Label | RealmPanelNode/ToastLabel |

### 5.4 验证

- 打开面板自动请求 `GET /character/info`，显示当前/下一境界与突破条件（境界名为 V2 八大境界）。
- 点"修炼升级"→ 经验足够时境界+1；升阶成功且响应携带 `unassigned_points` 时，面板会广播 `POINTS_UPDATED` 事件同步给加点面板（见第九章）。
- 点"境界突破"→ 大境界突破，AnimNode 播放金光缩放动画。
- 神魔境界（major_stage=8）时 DaoSection 显示；道值≥100 时 DaoProgressLabel 变金色，点"子阶突破"→ `POST /character/dao/breakthrough` 成功后显示"已破入 神魔·XX"，并广播 `ATTR_UPDATED` 让角色面板刷新（子阶突破发放固定点）。
- 非神魔境界点"子阶突破"→ Toast 提示"只有神魔境界才能进行子阶突破"（本地拦截，不发请求）。
- 突破失败（心魔/条件不足）时 ResultLabel 显示红色失败原因。

---

## 第六章 花果山副本三件套（入口/战斗/结算）

> 状态：**两个 Prefab 已存在**（`assets/prefabs/ui/HuaguoshanEntryPanel.prefab`、`HuaguoshanSettlePanel.prefab`），
> 战斗层 HgsBattleNode 需在 Hall.scene 中新建。
> 事件链：EntryUI 发 `hgs:enter` → HuaguoshanBattle 监听并接管画面 → 战斗结束发 `hgs:settle` → SettleUI 弹出。

### 6.1 入口面板（HuaguoshanEntryPanel.prefab 拖入场景）

1. 将 `assets/prefabs/ui/HuaguoshanEntryPanel.prefab` 拖到 Hall.scene 的 Canvas 下，重命名保持 `HuaguoshanEntryPanel`，初始 active=false。
2. Prefab 内部节点树（已存在，核对即可）：

```
Panel                               ← 挂 HuaguoshanEntryUI 组件
├── TitleLabel                      (Label "花果山")
├── DescLabel                       (Label，副本描述)
├── LuckLabel                       (Label "今日气运: --")
├── FatigueLabel                    (Label "疲劳值: --/--")
├── ProbsContainer                  (Node，六角色概率区)
│   ├── ProbWukongLabel             (Label "孙悟空: --%")
│   ├── ProbMowangLabel             (Label "魔王: --%")
│   ├── ProbShouxaLabel             (Label "兽侠: --%")
│   ├── ProbMonkeyLabel             (Label "普通猴: --%")
│   ├── ProbOldMonLabel             (Label "老猴: --%")
│   └── ProbGrassLabel              (Label "草木: --%")
├── ButtonsContainer                (Node)
│   ├── EnterBtn                    (Node+Sprite "进入副本")
│   ├── MeditateBtn                 (Node+Sprite "打坐回复")
│   └── CloseBtn                    (Node+Sprite "×")
└── ToastLabel                      (Label，初始 active=false)
```

3. 若 Prefab 绑定丢失，按下表重绑（选中 Panel → HuaguoshanEntryUI，共 14 项）：

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| titleLabel | Label | Panel/TitleLabel |
| descLabel | Label | Panel/DescLabel |
| luckLabel | Label | Panel/LuckLabel |
| fatigueLabel | Label | Panel/FatigueLabel |
| probWukongLabel | Label | ProbsContainer/ProbWukongLabel |
| probMowangLabel | Label | ProbsContainer/ProbMowangLabel |
| probShouxaLabel | Label | ProbsContainer/ProbShouxaLabel |
| probMonkeyLabel | Label | ProbsContainer/ProbMonkeyLabel |
| probOldMonLabel | Label | ProbsContainer/ProbOldMonLabel |
| probGrassLabel | Label | ProbsContainer/ProbGrassLabel |
| enterBtn | Node | ButtonsContainer/EnterBtn |
| meditateBtn | Node | ButtonsContainer/MeditateBtn |
| closeBtn | Node | ButtonsContainer/CloseBtn |
| toastLabel | Label | Panel/ToastLabel |

### 6.2 战斗层 HgsBattleNode（需新建）

1. Hall.scene 中 Canvas 下创建空节点 `HgsBattleNode`（Sibling Index 放最高层，见第八章），挂 `HuaguoshanBattle` 组件（`scripts/scene/Huaguoshan/HuaguoshanBattle.ts`）。
2. 节点树：

```
Canvas
└── HgsBattleNode                   ← 挂 HuaguoshanBattle 组件（本节点常驻 active=true，内部 BattleLayer 默认隐藏）
    └── BattleLayer                 (Node，初始 active=false，战斗时脚本打开)
        ├── BattleBg                (Sprite，全屏铺满，深绿 #0f3d2e)
        ├── TimerLabel              (Label "10:00"，Position (0, 290)，32号)
        ├── RoleNameLabel           (Label，抽中的角色名，Position (0, 240))
        ├── HpBarNode               (Node，血条容器，Position (0, 200))
        │   ├── BarBg               (Sprite，400×20)
        │   └── BarFill             (Sprite，锚点X=0，脚本控制)
        ├── ObserveBarNode          (Node，观想进度条，Position (0, 170)，结构同上)
        ├── EventButtonsNode        (Node，事件按钮容器，Position (0, -220))
        ├── WukongUltimateBtn       (Node+Sprite "如意金箍"，Position (-300, -260)，初始 active=false)
        ├── MowangSummonBtn         (Node+Sprite "召唤小妖"，Position (-100, -260)，初始 active=false)
        ├── MowangBurstBtn          (Node+Sprite "魔气爆发"，Position (100, -260)，初始 active=false)
        └── MowangDeathBtn          (Node+Sprite "殊死一搏"，Position (300, -260)，初始 active=false)
```

3. Inspector 绑定表（选中 HgsBattleNode → HuaguoshanBattle，共 12 项）：

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| battleLayer | Node | HgsBattleNode/BattleLayer |
| hallWorldLayer | Node | **Canvas/WorldNode**（战斗时隐藏大厅世界层） |
| timerLabel | Label | BattleLayer/TimerLabel |
| roleNameLabel | Label | BattleLayer/RoleNameLabel |
| hpBarNode | Node | BattleLayer/HpBarNode |
| observeBarNode | Node | BattleLayer/ObserveBarNode |
| eventButtonsNode | Node | BattleLayer/EventButtonsNode |
| wukongUltimateBtn | Node | BattleLayer/WukongUltimateBtn |
| mowangSummonBtn | Node | BattleLayer/MowangSummonBtn |
| mowangBurstBtn | Node | BattleLayer/MowangBurstBtn |
| mowangDeathBtn | Node | BattleLayer/MowangDeathBtn |
| (无字段的 BattleBg 等) | - | 仅作显示，无需绑定 |

### 6.3 结算面板（HuaguoshanSettlePanel.prefab 拖入场景）

1. 将 `assets/prefabs/ui/HuaguoshanSettlePanel.prefab` 拖到 Canvas 下，初始 active=false。
2. Prefab 内部节点树（已存在）：

```
Panel                               ← 挂 HuaguoshanSettleUI 组件
├── TitleLabel                      (Label "副本结算")
├── RoleLabel                       (Label，本局角色)
├── OutcomeLabel                    (Label，胜负结果)
├── GradeLabel                      (Label，评级)
├── DurationLabel                   (Label，用时)
├── ObserveLabel                    (Label，观想进度)
├── DeathTextLabel                  (Label，死亡文案)
├── CommentLabel                    (Label，评语)
├── RewardSection                   (Node)
│   ├── RewardListRoot              (Node，奖励条目容器，脚本动态填充)
│   └── RewardSummaryLabel          (Label，奖励汇总)
├── ConfirmBtn                      (Node+Sprite "确认返回")
└── ToastLabel                      (Label，初始 active=false)
```

3. 绑定表（选中 Panel → HuaguoshanSettleUI，共 12 项）：

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| titleLabel | Label | Panel/TitleLabel |
| roleLabel | Label | Panel/RoleLabel |
| outcomeLabel | Label | Panel/OutcomeLabel |
| gradeLabel | Label | Panel/GradeLabel |
| durationLabel | Label | Panel/DurationLabel |
| observeLabel | Label | Panel/ObserveLabel |
| deathTextLabel | Label | Panel/DeathTextLabel |
| commentLabel | Label | Panel/CommentLabel |
| rewardListRoot | Node | RewardSection/RewardListRoot |
| rewardSummaryLabel | Label | RewardSection/RewardSummaryLabel |
| confirmBtn | Node | Panel/ConfirmBtn |
| toastLabel | Label | Panel/ToastLabel |

### 6.4 入口接线与验证

- 接线：HallUI 的功能按钮"副本"点击 → `HuaguoshanEntryPanel` 节点 active=true（见第八章 8.2 接线表）。
- 验证链路（需 dungeon-service 8007 启动）：
  1. 大厅点"副本"→ 入口面板显示气运/疲劳/六角色概率。
  2. 点"进入副本"→ `POST /dungeon/hgs/enter` → 发 `hgs:enter` 事件 → WorldNode 隐藏、BattleLayer 显示，倒计时开始。
  3. 抽中孙悟空显示"如意金箍"按钮；抽中魔王显示三个魔王技能按钮。
  4. 战斗结束（胜利/死亡/超时）→ `hgs:settle` 事件 → 结算面板弹出奖励列表。
  5. 点"确认返回"→ BattleLayer 隐藏、WorldNode 恢复。

---

## 第七章 死亡系统四面板（六道/兽身/夺舍/公敌）

> 状态：**四个脚本均已存在（`scripts/ui/`），需在 Hall.scene 新建节点。**
> 触发：角色死亡后由 death-service(8006) 驱动；DeathChoiceUI 由 PLAYER_DEATH 事件弹出。

### 7.1 六道选择面板 DeathChoiceUI

节点树（Canvas 下新建）：

```
Canvas
└── DeathChoicePanelNode            (Node，初始 active=false) ← 挂 DeathChoiceUI 组件
    ├── Mask                        (Sprite，全屏黑 220 透明度，加 BlockInputEvents)
    ├── PanelBg                     (Sprite，640×520)
    ├── TitleLabel                  (Label "身死道消，轮回抉择")
    ├── KarmaHintLabel              (Label，业力提示)
    ├── PathsContainer              (Node，2列3行按钮阵)
    │   ├── TianDaoBtn              (Node+Sprite，"天道"，金色 #ffd700)
    │   ├── XiuluoDaoBtn            (Node+Sprite，"修罗道"，红色 #d94141)
    │   ├── RenDaoBtn               (Node+Sprite，"人道"，绿色 #41d97a)
    │   ├── ChushengDaoBtn          (Node+Sprite，"畜生道"，棕色 #a0762e)
    │   ├── EguiDaoBtn              (Node+Sprite，"饿鬼道"，紫色 #8e41d9)
    │   └── DiyuDaoBtn              (Node+Sprite，"地狱道"，深灰 #4a4a5a)
    ├── GhostBtn                    (Node+Sprite，"化作阴魂"，Position (0, -180))
    ├── ConfirmBtn                  (Node+Sprite，"确认"，Position (0, -240)，初始 active=false)
    ├── ResultLabel                 (Label，轮回结果，初始 active=false)
    └── ToastLabel                  (Label，初始 active=false)
```

布局：PathsContainer 内按钮 2 列（x=-150/150）3 行（y=100/20/-60），每个 160×64。

绑定表（共 12 项）：

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| titleLabel | Label | DeathChoicePanelNode/TitleLabel |
| karmaHintLabel | Label | DeathChoicePanelNode/KarmaHintLabel |
| tianDaoBtn | Node | PathsContainer/TianDaoBtn |
| xiuluoDaoBtn | Node | PathsContainer/XiuluoDaoBtn |
| renDaoBtn | Node | PathsContainer/RenDaoBtn |
| chushengDaoBtn | Node | PathsContainer/ChushengDaoBtn |
| eguiDaoBtn | Node | PathsContainer/EguiDaoBtn |
| diyuDaoBtn | Node | PathsContainer/DiyuDaoBtn |
| ghostBtn | Node | DeathChoicePanelNode/GhostBtn |
| confirmBtn | Node | DeathChoicePanelNode/ConfirmBtn |
| resultLabel | Label | DeathChoicePanelNode/ResultLabel |
| toastLabel | Label | DeathChoicePanelNode/ToastLabel |

### 7.2 兽身面板 BeastTransformUI

节点树：

```
Canvas
└── BeastPanelNode                  (Node，初始 active=false) ← 挂 BeastTransformUI 组件
    ├── Mask + PanelBg              (480×360)
    ├── TitleLabel                  (Label "兽身修行")
    ├── CurrentFormLabel            (Label "当前形态: --")
    ├── BeastTypeLabel              (Label "兽类: --")
    ├── EvolutionLabel              (Label "进化度: --")
    ├── JingBonusLabel              (Label "精加成: --")
    ├── TransformBtn                (Node+Sprite)
    │   └── Label                   (←绑定 transformBtnLabel)
    ├── ResultLabel                 (Label，初始 active=false)
    ├── CloseBtn                    (Node，脚本按名称查找)
    └── ToastLabel                  (Label，初始 active=false)
```

绑定表（共 8 项）：currentFormLabel / beastTypeLabel / evolutionLabel / jingBonusLabel / resultLabel（各 Label 对应同名节点）、transformBtn(Node→TransformBtn)、transformBtnLabel(Label→TransformBtn/Label)、toastLabel。

### 7.3 夺舍面板 PossessUI

节点树：

```
Canvas
└── PossessPanelNode                (Node，初始 active=false) ← 挂 PossessUI 组件
    ├── Mask + PanelBg              (520×420)
    ├── TitleLabel                  (Label "夺舍")
    ├── TargetNameLabel             (Label "目标: --")
    ├── TargetShenLabel             (Label "目标神识: --")
    ├── SuccessRateLabel            (Label "成功率: --%")
    ├── PossessCountLabel           (Label "剩余次数: --")
    ├── PossessBtn                  (Node+Sprite "夺舍")
    ├── AnimNode                    (Node，夺舍动画容器，初始 active=false)
    ├── ResultLabel                 (Label，初始 active=false)
    ├── CloseBtn                    (Node，脚本按名称查找)
    └── ToastLabel                  (Label，初始 active=false)
```

绑定表（共 9 项）：titleLabel / targetNameLabel / targetShenLabel / successRateLabel / resultLabel / possessCountLabel（各对应同名 Label）、possessBtn(Node)、animNode(Node)、toastLabel。
目标信息由外部代码调用 `setTarget(type, id, name, shen)` 传入，无需在编辑器配置。

### 7.4 天道公敌指示器 PublicEnemyIndicator（常驻 HUD）

节点树（挂在 HallUINode 下，**非弹窗**，常驻显示）：

```
HallUINode
└── PublicEnemyNode                 (Node，Position (0, 280)) ← 挂 PublicEnemyIndicator 组件
    ├── EnemyIcon                   (Node+Sprite，红色警示图标，初始 active=false)
    ├── BountyLabel                 (Label "悬赏: --")
    ├── ThunderCountLabel           (Label "天雷: --")
    ├── ThunderWarningLabel         (Label，天雷预警，初始 active=false)
    └── StatusLabel                 (Label，公敌状态)
```

绑定表（共 5 项）：enemyIcon(Node)、bountyLabel / thunderCountLabel / thunderWarningLabel / statusLabel（各对应同名 Label）。
组件每 10 秒自动轮询公敌状态；非公敌玩家所有子节点隐藏，无副作用。

---

## 第八章 面板层级与穿透控制

### 8.1 Hall.scene 的 Canvas 子节点 Sibling Index（从下到上）

| Index | 节点 | 说明 |
|---|---|---|
| 0 | WorldNode | 世界层（地面/实体/相机） |
| 1 | HallUINode | 常驻 HUD（含 PublicEnemyNode、AbnormalStatusNode） |
| 2 | CharacterPanelNode | 角色面板 |
| 3 | RealmPanelNode | 境界面板 |
| 4 | AttributeAllocationNode | V2 加点面板（比角色面板高一层，见第九章） |
| 5 | WashPointNode | V2 洗点确认框（比加点面板高一层，见第九章） |
| 6 | BeastPanelNode / PossessPanelNode | 死亡系统小面板 |
| 7 | DeathChoicePanelNode | 六道面板（死亡时最优先） |
| 8 | HuaguoshanEntryPanel / HgsBattleNode / HuaguoshanSettlePanel | 副本三件套（最高层） |

调整方法：层级管理器中直接拖动节点顺序（列表越靠下渲染越靠上）。

### 8.2 功能按钮接线表

HallUI 的 `funcEntryContainer` 中的功能按钮由脚本动态创建，点击后发出对应事件/直接开面板。需要在 `HallUI.ts` 的按钮回调中接线（代码已有 TODO 位置或按以下对应关系补全）：

| 功能按钮 | 打开的面板节点 | 方式 |
|---|---|---|
| 角色 | CharacterPanelNode | `getComponent(CharacterPanel).show()` |
| 功法 | RealmPanelNode | `active = true`（组件 onEnable 自动拉数据） |
| 副本 | HuaguoshanEntryPanel | `active = true` |

> 若当前 HallUI 版本已内置事件派发（`EventManager.emit`），则只需确认面板脚本监听的事件名一致，无需改代码。

### 8.3 穿透控制规则

- 所有弹窗面板的 `Mask` 节点**必须**添加 `BlockInputEvents` 组件，否则点击会穿透到大厅地面导致角色移动。
- BattleLayer 的 BattleBg 铺满全屏即天然阻挡（Sprite 有效区域接收触摸），无需额外组件。
- 常驻 HUD（PublicEnemyNode）不加 BlockInputEvents，避免遮挡地面点击。

---

## 第九章 人物属性 V2 三大新面板（加点/洗点/异常状态）

> 状态：**三个脚本均已存在（`scripts/ui/`），需在 Hall.scene 新建节点。**
> 脚本：`AttributeAllocationUI.ts`（加点面板）、`WashPointUI.ts`（洗点确认框）、`AbnormalStatusUI.ts`（异常状态栏）。
> 打开方式：全部由 `EventManager` 事件驱动，**没有任何按钮直接引用这三个面板**：
> - 加点面板 ← 监听 `CharacterV2Event.OPEN_ALLOCATE`（角色面板"加点"按钮触发）
> - 洗点确认框 ← 监听 `CharacterV2Event.OPEN_WASH`（角色面板"洗点"按钮触发）
> - 异常状态栏 ← 监听 `CharacterV2Event.ABNORMAL_TRIGGERED`（战斗逻辑广播判定结果）

> ⚠️注意（三个面板通用，最容易踩的坑）：这三个面板节点在场景中**必须保持初始激活（Inspector 勾选 active）**，与第四~七章"弹窗初始取消勾选"的惯例相反！
> 原因：事件监听注册写在组件 `onLoad()` 里，而 Cocos Creator 中**未激活节点的组件不会执行 onLoad**。若初始取消勾选，事件永远收不到，面板永远打不开。
> 正确做法：保持勾选 → 加点面板/洗点确认框的 `onLoad()` 注册完事件后会自行执行 `this.node.active = false` 隐藏；异常状态栏则常驻显示（内部三个图标自行隐藏）。

### 9.1 加点面板 AttributeAllocationUI

功能：精/气/神各一行（当前值 + "+/-"按钮 + 本次待分配数），显示剩余可分配点数、均衡加成实时预判，确认后调用 `POST /character/points/allocate`。

#### 9.1.1 节点树（Hall.scene 的 Canvas 下新建）

```
Canvas
└── AttributeAllocationNode         (Node，保持勾选 active！) ← 挂 AttributeAllocationUI 组件
    ├── Mask                        (Sprite，全屏半透明黑 #000000 180，加 BlockInputEvents)
    ├── PanelBg                     (Sprite，560×480，深色底 #1e1e32)
    ├── TitleLabel                  (Label "属性加点"，24号，仅显示无需绑定)
    ├── JingRow                     (Node，精行容器)
    │   ├── JingCurrentLabel        (Label "精: --")
    │   ├── JingMinusBtn            (Node+Sprite "-"，48×48)
    │   ├── JingPendingLabel        (Label "+0")
    │   └── JingPlusBtn             (Node+Sprite "+"，48×48)
    ├── QiRow                       (Node，气行容器，结构同上)
    │   ├── QiCurrentLabel          (Label "气: --")
    │   ├── QiMinusBtn              (Node+Sprite "-")
    │   ├── QiPendingLabel          (Label "+0")
    │   └── QiPlusBtn               (Node+Sprite "+")
    ├── ShenRow                     (Node，神行容器，结构同上)
    │   ├── ShenCurrentLabel        (Label "神: --")
    │   ├── ShenMinusBtn            (Node+Sprite "-")
    │   ├── ShenPendingLabel        (Label "+0")
    │   └── ShenPlusBtn             (Node+Sprite "+")
    ├── RemainLabel                 (Label "剩余可分配: 0")
    ├── EquilibriumTipLabel         (Label，均衡预判提示，脚本自动填绿色/红色文案)
    ├── ConfirmBtn                  (Node+Sprite "确认加点"，180×56)
    │   └── Label
    ├── CancelBtn                   (Node+Sprite "取消"，180×56)
    │   └── Label
    └── ToastLabel                  (Label，初始 active=false)
```

#### 9.1.2 搭建步骤

1. Canvas 下创建空节点 `AttributeAllocationNode`，Position (0,0)，**保持 active 勾选**。
2. 添加组件 → 自定义脚本 → `AttributeAllocationUI`。
3. 按上面节点树创建子节点。推荐布局（PanelBg 内相对坐标）：
   - TitleLabel (0, 200)
   - JingRow (0, 120) / QiRow (0, 60) / ShenRow (0, 0)；每行内：CurrentLabel (-180, 0)、MinusBtn (-40, 0)、PendingLabel (30, 0)、PlusBtn (100, 0)
   - RemainLabel (0, -70)、EquilibriumTipLabel (0, -110)
   - ConfirmBtn (-110, -180)、CancelBtn (110, -180)
   - ToastLabel (0, -230)
4. Mask 加 `BlockInputEvents` 组件。
5. 层级：放在 RealmPanelNode 之上（见第八章 8.1，Sibling Index 4）——保证从角色面板点"加点"弹出时盖在角色面板上面。

> ⚠️注意（待分配点数从哪来）：后端**没有单独查询待分配点数的 GET 接口**。加点面板的待分配余额完全靠 `POINTS_UPDATED` 事件同步——升阶（`/character/realm/upgrade`）、加点、洗点三个接口的响应里都携带 `unassigned_points`，对应面板成功后会广播该事件。因此：**角色首次打开加点面板、且从未升过阶时，"剩余可分配"显示 0 是正常的**；先去境界面板升一阶（发放自由点）再回来即可看到余额。

#### 9.1.3 Inspector 绑定表（选中 AttributeAllocationNode → AttributeAllocationUI 组件，共 17 项）

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| jingCurrentLabel | Label | JingRow/JingCurrentLabel |
| jingPendingLabel | Label | JingRow/JingPendingLabel |
| jingPlusBtn | Node | JingRow/JingPlusBtn |
| jingMinusBtn | Node | JingRow/JingMinusBtn |
| qiCurrentLabel | Label | QiRow/QiCurrentLabel |
| qiPendingLabel | Label | QiRow/QiPendingLabel |
| qiPlusBtn | Node | QiRow/QiPlusBtn |
| qiMinusBtn | Node | QiRow/QiMinusBtn |
| shenCurrentLabel | Label | ShenRow/ShenCurrentLabel |
| shenPendingLabel | Label | ShenRow/ShenPendingLabel |
| shenPlusBtn | Node | ShenRow/ShenPlusBtn |
| shenMinusBtn | Node | ShenRow/ShenMinusBtn |
| remainLabel | Label | AttributeAllocationNode/RemainLabel |
| equilibriumTipLabel | Label | AttributeAllocationNode/EquilibriumTipLabel |
| confirmBtn | Node | AttributeAllocationNode/ConfirmBtn |
| cancelBtn | Node | AttributeAllocationNode/CancelBtn |
| toastLabel | Label | AttributeAllocationNode/ToastLabel |

（精/气/神三组各 4 项共 12 项 + 汇总 2 项 + 确认/取消 2 项 + Toast 1 项 = 17 个 @property，逐行核对勿漏。）

#### 9.1.4 验证

- 角色面板点"加点"→ 本面板弹出，自动请求 `GET /character/attributes` 显示当前精/气/神。
- 点 "+" 待分配数 +1、剩余数 -1；剩余为 0 时再点 "+" → Toast "待分配点数不足"。
- 均衡预判：按"当前值+本次待分配"计算，max÷min≤3 时提示绿色"均衡加成生效 ×2"，否则红色"偏科Build 均衡加成未激活 ×1"（实时变化）。
- 点"确认加点"（至少分配 1 点）→ `POST /character/points/allocate` 成功 → Toast "加点成功"，并广播 `ATTR_UPDATED` + `POINTS_UPDATED`（角色面板同步刷新）。
- 点"取消"→ 清空本次待分配并关闭面板。

### 9.2 洗点确认框 WashPointUI

功能：确认后调用 `POST /character/points/wash`，把已分配的自由点全部返还到待分配池（固定点不动），并展示返还明细与灵石费用。

#### 9.2.1 节点树（Hall.scene 的 Canvas 下新建）

```
Canvas
└── WashPointNode                   (Node，保持勾选 active！) ← 挂 WashPointUI 组件
    ├── Mask                        (Sprite，全屏半透明黑，加 BlockInputEvents)
    ├── PanelBg                     (Sprite，480×340，深色底)
    ├── TitleLabel                  (Label "洗点"，仅显示无需绑定)
    ├── TipLabel                    (Label，多行，洗点说明，脚本打开时自动填文案)
    ├── ResultLabel                 (Label，多行，洗点结果明细，成功后脚本填绿色明细)
    ├── ConfirmBtn                  (Node+Sprite "确认洗点"，160×56)
    │   └── Label
    ├── CancelBtn                   (Node+Sprite "取消"，160×56)
    │   └── Label
    └── ToastLabel                  (Label，初始 active=false)
```

#### 9.2.2 搭建步骤

1. Canvas 下创建空节点 `WashPointNode`，**保持 active 勾选**（onLoad 注册 `OPEN_WASH` 监听后自行隐藏）。
2. 挂 `WashPointUI` 组件。
3. 布局：TitleLabel (0,130)、TipLabel (0,50)（行高 30，启用换行）、ResultLabel (0,-30)、ConfirmBtn (-90,-110)、CancelBtn (90,-110)、ToastLabel (0,-160)。
4. Mask 加 `BlockInputEvents`；层级放在加点面板之上（Sibling Index 5）。

#### 9.2.3 Inspector 绑定表（选中 WashPointNode → WashPointUI 组件，共 5 项）

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| tipLabel | Label | WashPointNode/TipLabel |
| resultLabel | Label | WashPointNode/ResultLabel |
| confirmBtn | Node | WashPointNode/ConfirmBtn |
| cancelBtn | Node | WashPointNode/CancelBtn |
| toastLabel | Label | WashPointNode/ToastLabel |

#### 9.2.4 验证

- 角色面板点"洗点"→ 确认框弹出，TipLabel 显示"洗点将返还所有已分配的自由属性点…"提示。
- 点"确认洗点"→ 成功后 ResultLabel 绿色显示"返还自由点：精+X 气+X 神+X / 共返还 X 点，当前待分配 X 点 / 灵石花费：X"，2 秒后自动关闭；同时广播 `ATTR_UPDATED` + `POINTS_UPDATED`。
- 没有已分配自由点时 → Toast 显示后端错误提示。
- 请求进行中重复点"确认"会被脚本忽略（防重复提交），属正常。

### 9.3 异常状态栏 AbnormalStatusUI（常驻 HUD，非弹窗）

功能：冰冻/灼烧/眩晕三个状态图标位 + 剩余秒数倒计时，到时自动消失。由 `POST /combat/abnormal` 的判定结果驱动（`triggered=true` 时显示）。

#### 9.3.1 节点树（挂在 HallUINode 下，与 PublicEnemyNode 同级）

```
HallUINode
└── AbnormalStatusNode              (Node，Position (0, 320)，保持勾选 active！) ← 挂 AbnormalStatusUI 组件
    ├── FreezeSlot                  (Node，冰冻位，Position (-80, 0))
    │   ├── FreezeIcon              (Node+Sprite，冰蓝色方块 48×48，代表冰冻图标)
    │   └── FreezeCountdownLabel    (Label "3s"，位于图标下方 (0,-36))
    ├── BurnSlot                    (Node，灼烧位，Position (0, 0))
    │   ├── BurnIcon                (Node+Sprite，橙红色方块 48×48)
    │   └── BurnCountdownLabel      (Label，同上)
    └── StunSlot                    (Node，眩晕位，Position (80, 0))
        ├── StunIcon                (Node+Sprite，黄色方块 48×48)
        └── StunCountdownLabel      (Label，同上)
```

#### 9.3.2 搭建步骤

1. HallUINode 下创建空节点 `AbnormalStatusNode`，**保持 active 勾选**（常驻 HUD）。
2. 挂 `AbnormalStatusUI` 组件，按上面节点树创建三组"图标+倒计时"。
3. 三个 Icon 的初始 active 勾选与否均可——`onLoad()` 会统一按剩余时间刷新为隐藏。
4. 倒计时 Label 挂在各 Slot 下、**不要挂在 Icon 节点内部**（Icon 隐藏时脚本会单独清空 Label 文字，二者独立控制）。
5. 常驻 HUD 不加 BlockInputEvents（同 PublicEnemyNode 规则，避免遮挡地面点击）。

#### 9.3.3 Inspector 绑定表（选中 AbnormalStatusNode → AbnormalStatusUI 组件，共 6 项）

| @property 字段 | 类型 | 拖入节点 |
|---|---|---|
| freezeIcon | Node | FreezeSlot/FreezeIcon |
| freezeCountdownLabel | Label | FreezeSlot/FreezeCountdownLabel |
| burnIcon | Node | BurnSlot/BurnIcon |
| burnCountdownLabel | Label | BurnSlot/BurnCountdownLabel |
| stunIcon | Node | StunSlot/StunIcon |
| stunCountdownLabel | Label | StunSlot/StunCountdownLabel |

#### 9.3.4 验证

- 战斗逻辑调用 `CharacterApi.combatAbnormal(...)` 拿到 `triggered=true` 的响应后 `EventManager.emit(CharacterV2Event.ABNORMAL_TRIGGERED, res.data)` → 对应图标亮起并显示"Ns"倒计时，每秒 -1，归零后图标与文字自动消失。
- 也可在控制台直接调用组件 `showAbnormal(1, 3)` 手动验证（1冰冻 2灼烧 3眩晕）。
- 同类型异常重复触发时，取剩余时间更长的那个（刷新持续时间而非叠加）。

---

## 第十章 V2 接口一览与 CharacterV2Event 事件总线

### 10.1 V2 接口一览（全部走 8005 端口 character-service，统一响应 `{code, msg, data}`）

`HttpClient._getServiceBaseUrl()` 已将 `/character/*` 与 `/combat/*` 前缀自动路由到 `DEV_CHARACTER_URL`（http://127.0.0.1:8005），前端调用时**只写路径即可**，无需关心端口。全部封装在 `scripts/net/CharacterApi.ts`：

| # | 方法 | 接口 | 请求参数 | 用途 |
|---|---|---|---|---|
| 1 | `CharacterApi.allocatePoints` | POST /character/points/allocate | `{character_id, jing, qi, shen}` | 分配自由属性点 |
| 2 | `CharacterApi.washPoints` | POST /character/points/wash | `{character_id}` | 洗点（返还全部已分配自由点，固定点不动） |
| 3 | `CharacterApi.daoGain` | POST /character/dao/gain | `{character_id, dao_type(1-5), amount}` | 积攒神魔之道（1太极 2太素 3太始 4太初 5太易） |
| 4 | `CharacterApi.daoBreakthrough` | POST /character/dao/breakthrough | `{character_id}` | 神魔子阶突破（道值≥100 消耗100 子阶+1） |
| 5 | `CharacterApi.getShield` | GET /character/shield | `?character_id=` | 查询护盾/气血状态 |
| 6 | `CharacterApi.combatSkill` | POST /combat/skill | `{attacker_id, defender_id, skill_type(1武器 2五行 3神位), skill_level(1-10), element_type(五行技必填1-5)}` | 技能伤害结算（先扣盾后扣血） |
| 7 | `CharacterApi.combatAbnormal` | POST /combat/abnormal | `{attacker_id, defender_id, abnormal_type(1冰冻 2灼烧 3眩晕)}` | 异常状态判定与施加 |
| 8 | `CharacterApi.shieldRecover` | POST /combat/shield/recover | `{character_id, seconds}` | 脱战护盾恢复结算 |
| 9 | （修复中新增） | POST /combat/burn/tick | `{character_id}` | 灼烧持续伤害逐秒结算（后端修复中，前端暂未封装） |

错误约定：余额不足/非神魔境界/道值不足等业务错误，后端返回 HTTP 400 且 `res.code !== 0`，各面板直接 Toast 展示 `res.msg`。

枚举（`CharacterApi.ts` 内导出，直接 import 使用）：

| 枚举 | 取值 |
|---|---|
| `DaoType` | Taiji=1 太极 / Taisu=2 太素 / Taishi=3 太始 / Taichu=4 太初 / Taiyi=5 太易 |
| `SkillType` | Weapon=1 武器技（吃体魄） / Element=2 五行技（吃功法威力+五行亲和） / Divine=3 神位技（吃神识） |
| `AbnormalType` | Freeze=1 冰冻 / Burn=2 灼烧 / Stun=3 眩晕 |

### 10.2 CharacterV2Event 事件总线（面板间通信全靠它）

项目规范：按钮触发的跨面板动作一律走 `EventManager`，禁止面板间直接 onClick 回调。事件常量定义在 `CharacterApi.ts` 的 `CharacterV2Event` 枚举中：

| 事件常量 | 事件名字符串 | 谁 emit（触发时机） | 谁监听（做什么） |
|---|---|---|---|
| `OPEN_ALLOCATE` | `charv2:open_allocate` | CharacterPanel"加点"按钮点击（可携带 unassigned_points 参数） | AttributeAllocationUI → 打开加点面板并拉取当前属性 |
| `OPEN_WASH` | `charv2:open_wash` | CharacterPanel"洗点"按钮点击 | WashPointUI → 打开洗点确认框 |
| `ATTR_UPDATED` | `charv2:attr_updated` | 加点成功 / 洗点成功 / 神魔子阶突破成功（携带最新接口响应数据） | CharacterPanel → 重新拉取全部数据刷新 |
| `POINTS_UPDATED` | `charv2:points_updated` | 升阶发点（RealmBreakthroughUI）/ 加点扣点 / 洗点返还成功（参数：unassigned_points 数值） | AttributeAllocationUI → 更新"剩余可分配"余额 |
| `DAO_UPDATED` | `charv2:dao_updated` | CharacterApi 封装层：`daoGain` 请求成功（code=0 且有 data）后自动广播（携带 DaoGainData，含全部 5 种道值） | RealmBreakthroughUI → 缓存道值并刷新道进度 |
| `ABNORMAL_TRIGGERED` | `charv2:abnormal_triggered` | CharacterApi 封装层：`combatAbnormal` 请求成功后自动广播（携带 CombatAbnormalData） | AbnormalStatusUI → `triggered=true` 时亮图标并倒计时 |

事件数据流转示意（新手理解用）：

```
[境界面板] 升阶成功(响应带unassigned_points) ──POINTS_UPDATED──▶ [加点面板] 余额更新
[角色面板] 点"加点" ──OPEN_ALLOCATE──▶ [加点面板] 弹出
[加点面板] 确认加点成功 ──ATTR_UPDATED──▶ [角色面板] 刷新
                      └─POINTS_UPDATED──▶ [加点面板自身] 余额更新
[角色面板] 点"洗点" ──OPEN_WASH──▶ [洗点确认框] 弹出
[洗点确认框] 确认洗点成功 ──ATTR_UPDATED + POINTS_UPDATED──▶ 同上
[战斗逻辑] daoGain成功 ──DAO_UPDATED──▶ [境界面板] 道进度刷新
[战斗逻辑] combatAbnormal触发 ──ABNORMAL_TRIGGERED──▶ [异常状态栏] 图标+倒计时
```

---

## 第十一章 编译、预览与联调验证

### 11.1 TypeScript 编译检查

```powershell
cd e:\ziji-xiaochengxu\frontend
npx tsc --noEmit
```

预期：仅 LoginPanel.ts 存在 2 个预先存在的类型误报（不影响运行），其余无错误。

### 11.2 十一步冒烟清单

| # | 步骤 | 预期结果 |
|---|---|---|
| 1 | 打开 Login.scene 预览 | 金色标题呼吸动画；服务器状态在线 |
| 2 | 点微信登录（开发模式模拟） | 1.5s 后跳 CharacterCreate（无角色）或 Hall（有角色） |
| 3 | 创建角色三步流程 | 性别→属性/命名→动画→进入 Hall |
| 4 | Hall 中点击地面 | 角色移动 + 相机跟随；NPC 可对话 |
| 5 | 点"角色"按钮 | CharacterPanel 弹出并填充 30 项数值（含 V2 护盾/亲和/反应/异常抵抗/均衡） |
| 6 | 点"功法"按钮 | RealmPanel 弹出，修炼/突破可用；非神魔境界道进度区隐藏 |
| 7 | 角色面板点"加点" | 加点面板弹出，±调整实时刷新均衡预判；确认后角色面板同步刷新 |
| 8 | 角色面板点"洗点" | 洗点确认框弹出；确认后显示返还明细，2 秒后自动关闭 |
| 9 | 触发 combatAbnormal（curl 或战斗逻辑） | 异常状态栏对应图标亮起并倒计时消失 |
| 10 | 点"副本"按钮 → 进入花果山 | 战斗层接管 → 结算 → 返回大厅 |
| 11 | （测试）调用死亡接口后回大厅 | DeathChoice 六道面板弹出，可选轮回 |

### 11.3 常见问题

| 问题 | 原因与解决 |
|---|---|
| 面板打开点按钮没反应 | Mask 忘加 BlockInputEvents 反而不是原因——检查按钮节点是否有 UITransform 且尺寸>0 |
| 点弹窗空白处角色跟着走 | Mask 节点缺 BlockInputEvents 组件 |
| Inspector 拖不进 Label | @property 类型是 Label 时必须拖**带 Label 组件的节点**；类型是 Node 时拖节点本身 |
| 场景跳转报错 scene not found | 场景文件名与 SceneName 枚举不一致（必须 Login/CharacterCreate/Hall）且需加入 Build 场景列表 |
| 请求全部失败 | 对应端口后端服务未启动；检查 Constants.ts 中服务地址 |
| 点"加点"/"洗点"没有任何反应 | V2 三面板节点初始被取消勾选 active，onLoad 没执行导致事件没注册——**必须保持勾选**（见第九章 ⚠️注意） |
| 加点面板"剩余可分配"一直是 0 | 正常现象：待分配点靠 POINTS_UPDATED 事件同步，先在境界面板升一阶发放自由点（见 9.1.2 ⚠️注意） |
| 道进度显示 --/100 | 正常现象：道值靠 DAO_UPDATED 事件缓存，调用过 daoGain 后才有数值（见 5.2 ⚠️注意） |

---

## 第十二章 Inspector 绑定速查总表

| # | 组件 | 挂载节点 | 绑定数 | 状态 |
|---|---|---|---|---|
| 1 | LoginPanel | Login.scene/Canvas | 13 | ✅ 已搭建 |
| 2 | CharacterCreatePanel | CharacterCreate.scene/Canvas | 21 | ✅ 已搭建 |
| 3 | HallScene | Hall.scene/Canvas | 5 | ✅ 已搭建 |
| 4 | HallUI | Hall.scene/HallUINode | 8 | ✅ 已搭建 |
| 5 | CharacterPanel | CharacterPanelNode | 30（含 V2 新增 7） | 🔨 待搭建（第四章） |
| 6 | RealmBreakthroughUI | RealmPanelNode | 11（含 V2 新增 3） | 🔨 待搭建（第五章） |
| 7 | HuaguoshanEntryUI | HuaguoshanEntryPanel(Prefab) | 14 | ✅ Prefab 已有，拖入即可 |
| 8 | HuaguoshanBattle | HgsBattleNode | 12 | 🔨 待搭建（第六章 6.2） |
| 9 | HuaguoshanSettleUI | HuaguoshanSettlePanel(Prefab) | 12 | ✅ Prefab 已有，拖入即可 |
| 10 | DeathChoiceUI | DeathChoicePanelNode | 12 | 🔨 待搭建（第七章 7.1） |
| 11 | BeastTransformUI | BeastPanelNode | 8 | 🔨 待搭建（第七章 7.2） |
| 12 | PossessUI | PossessPanelNode | 9 | 🔨 待搭建（第七章 7.3） |
| 13 | PublicEnemyIndicator | HallUINode/PublicEnemyNode | 5 | 🔨 待搭建（第七章 7.4） |
| 14 | AttributeAllocationUI | AttributeAllocationNode | 17 | 🔨 待搭建（第九章 9.1，保持 active） |
| 15 | WashPointUI | WashPointNode | 5 | 🔨 待搭建（第九章 9.2，保持 active） |
| 16 | AbnormalStatusUI | HallUINode/AbnormalStatusNode | 6 | 🔨 待搭建（第九章 9.3，保持 active） |
| 17 | GameManager | 常驻节点（代码自建） | 0 | ✅ 无需手动操作 |

**合计 188 个绑定：已搭建 47 + Prefab 现成 26 + 待搭建 115（其中 V2 相关 38）。**

---

## 附录 待开发部分（野怪/装备 UI）

以下 API 层已完成、**尚无任何 UI 调用**，属于后续开发内容（本指南不含其搭建步骤）：

### A.1 野怪系统（MonsterApi，8008 monster-service，8 个方法）

`scripts/net/MonsterApi.ts`：listMonsters / getMonsterDetail / attackMonster / battleResult / listCaptured / capture / releaseCaptured / getWorldStatus。
建议 UI：大厅野怪实体渲染（复用 NPCEntity 模式）、战斗面板、灵兽袋面板。

### A.2 装备系统（EquipmentApi，8009 equipment-service，18 个方法）

`scripts/net/EquipmentApi.ts`：背包/穿戴/强化/打造/碎片合成/传承/交易行等 18 个方法。
建议 UI：背包面板（inventory）、装备详情弹窗、锻造炉面板、交易行面板。

### A.3 建议开发顺序

1. 先按本指南第四~七章、第九章完成现有脚本的面板搭建（打通全部已有功能，含人物属性 V2）。
2. 野怪：大厅显示野怪 → 点击战斗 → 战利品 Toast（最小闭环）；战斗结算可直接复用第十章的 `combatSkill`/`combatAbnormal` 接口。
3. 装备：背包面板 → 穿戴/卸下 → 属性联动 CharacterPanel。
4. 最后接交易行与传承等长线功能。

---

> 文档完。搭建过程中若发现脚本 @property 与本表不符，以 `assets/scripts/` 内最新代码为准。
